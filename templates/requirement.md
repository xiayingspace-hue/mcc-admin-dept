---
编号: REQ-XXX
模块: <slug>
端侧: admin | user
载体: shared | pc | h5 | native
状态: 草稿 | 评审中 | 已确认 | 已实现
---

# <模块中文名>（<slug>-XXX-<端侧>）

## 1. 概述

<做什么、为什么做、明确写不做什么（非目标）>

## 2. 涉及角色

引用 [`ROLES.md`](../../shared/ROLES.md)，本需求涉及：<角色列表>

## 3. 术语与模块归属

本需求使用的术语见 [`GLOSSARY.md`](../../shared/GLOSSARY.md)。新术语先登记再引用。

## 4. 使用场景

按角色列典型场景，写"谁在什么情况下想做什么"，不写 UI 操作步骤。

## 5. 业务流程与状态机

<有状态流转的对象，用表格列出：状态 → 触发条件 → 下一状态>

## 6. 数据对象

<业务语义定义：这个对象是什么、谁能看、什么时候会变。禁止出现字段名/类型/表结构。>

## 7. 权限规则

<格式：谁 + 在什么条件下 + 能做什么。本对象的唯一定义处。>

## 8. 校验规则

<格式：字段/动作 + 规则 + 触发时机。本对象的唯一定义处。>

## 9. 页面与状态清单

| 页面 | 需要的状态 |
|---|---|
| <页面名> | normal / loading / empty(未有过) / empty(筛选无果) / error / noperm / overflow |

## 10. 验收标准

- AC-001：<可独立测试的一句话>
- AC-002：……

## 11. 已知缺口与非目标

| 缺口 | 谁来定 | 何时前定 |
|---|---|---|
| <未定项> | <角色/人> | <里程碑> |
