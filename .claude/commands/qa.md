# /qa — 从验收标准生成测试用例

用法：`/qa <编号>`

步骤：
1. 读对应需求文档的「验收标准」「校验规则」章节。
2. 读 `rules/qa-rules.md`、`templates/qa-spec.md`。
3. 每条 AC 至少一条正向用例，隐含边界条件的额外派生负向/边界用例；每条校验规则至少一条用例。
4. 覆盖五态对应的可验证点（骨架屏、空态两种文案、错误重试、无权限拦截/脱敏、超长不塌陷）。
5. 写入 `outputs/qa/QA-<slug>-<编号>-<端侧>.md`，编号三位序号，按文档内出现顺序编，追加不重排。
